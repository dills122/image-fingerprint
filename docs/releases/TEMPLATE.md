# image-fingerprint VERSION

One or two sentences describing the user-facing purpose of this release and who should care.

## At a glance

- **Release type:** stable patch, stable minor, stable major, or prerelease
- **Package:** [`image-fingerprint@VERSION`](https://www.npmjs.com/package/image-fingerprint/v/VERSION)
- **Node.js:** supported versions
- **Headline:** the most important user-visible outcome

## Install or upgrade

```sh
npm install image-fingerprint@VERSION
```

Mention the package manager alternative or migration command only when it helps.

## What's changed

Describe behavior and user value, grouped by feature or theme. Link to supporting documentation when
details matter. Do not paste commit or pull-request titles here.

## Examples

Include a small, runnable example of the main new behavior. If a release has no API change, show the
existing workflow that benefits from the fix and explain the difference.

## Breaking changes

State **None** when the stable contract is unchanged. Otherwise list each break, affected users,
migration steps, and rollback considerations.

## Experimental

State **None** when there are no experimental changes. Otherwise identify every experimental
entrypoint or contract, stability expectations, persistence guidance, and known limits.

## Compatibility

Summarize supported runtimes, stable data-format impact, decoder or output changes, and any action
required for stored fingerprints.

## Full changelog

Link to the GitHub comparison for the previous and current tags. Add focused design or evidence links
only when they help a consumer understand the release.
